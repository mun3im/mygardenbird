# MyGardenBird pipeline dependencies
# Stages 1–9: https://doi.org/10.5281/zenodo.20306877
#
# Install:
#   pip install -r requirements.txt
#
# TensorFlow note: on macOS use tensorflow-macos + tensorflow-metal instead of tensorflow.
# BirdNET note: Stage 7 requires birdnet_analyzer (Cornell BirdNET-Analyzer ≥ 2.2).
#   pip install birdnet_analyzer
#   Then download model weights: python -m birdnet_analyzer.download_models

# ── data & I/O ───────────────────────────────────────────────────────────────
numpy>=1.26
scipy>=1.11
pandas>=2.0
soundfile>=0.12
sounddevice>=0.4          # Stage 4: audio playback in annotation GUI
librosa>=0.10
requests>=2.31
tqdm>=4.65

# ── machine learning ─────────────────────────────────────────────────────────
tensorflow>=2.15          # Stages 9 (training) and export_mobilenetv3s_tflite.py
scikit-learn>=1.3
PuLP>=2.8                 # Stage 8: MIP-based splitting

# ── visualisation ────────────────────────────────────────────────────────────
matplotlib>=3.8
seaborn>=0.13

# ── system / utils ───────────────────────────────────────────────────────────
psutil>=5.9               # Stage 9: resource monitoring

# ── BirdNET (Stage 7 only) ───────────────────────────────────────────────────
birdnet_analyzer>=2.2
